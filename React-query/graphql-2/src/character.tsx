import React from 'react';
// import {useGetMeQuery} from "./gql/generated/posts.generated";

const Character = () => {

    // const {data} = useGetMeQuery();
    // const {data: users} = useGetAppUsersQuery({getAppUsersArgs: {limit: 12, offset: 0}});
    // console.log(data, 'data');
    // console.log(users, 'users');
    return (
        <div>
            open console log
        </div>
    );
};

export default Character;