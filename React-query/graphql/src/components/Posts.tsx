import React from 'react';
// import {useCharactersQuery} from "./posts.generated";


const Posts = () => {
    // const {data, isLoading, isFetching} = useCharactersQuery({page: 1});
    // console.log({data});
    // console.log({isLoading});
    // console.log({isFetching});
    return (
        <div>
            posts
        </div>
    );
};

export default Posts;